
# Drive Share Hub V0.6

## 這次修正
1. 改為可一次選取多個檔案上傳（使用 `<input type="file" multiple>`），不使用拖曳、不使用資料夾選取。
2. Google 使用者登入介面統一移到「帳戶 / API 設定」頁面內。
3. 檔案中心保留登入狀態摘要，但不再放登入按鈕。
4. 多檔上傳採依序上傳，並以整批總進度顯示上傳百分比與目前檔名。
5. 維持既有 GitHub Pages、PWA、版本檢查、Shared Drive / 權限驗證功能。
